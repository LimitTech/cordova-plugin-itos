repositories{
    jcenter()
    flatDir{
        dirs 'src/main/libs'
    }
}

dependencies {
    implementation(name:'SDKItosDeviceLink', ext:'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}